services:
  erp-frontend:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "4200:4200"
    environment:
      - CHOKIDAR_USEPOLLING=true # 確保 Windows/Mac 檔案變更偵測正常
